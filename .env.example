# Local PostgreSQL (see compose.yaml). In production, use the Amazon RDS URL.
DATABASE_URL="postgresql://candidgarden:candidgarden@localhost:5432/candidgarden?schema=public"
# Tests reset their schemas, so this must point to a dedicated test database.
TEST_DATABASE_URL="postgresql://candidgarden:candidgarden@localhost:5432/candidgarden_test?schema=public"
SESSION_SECRET="super-duper-s3cret"
HONEYPOT_SECRET="super-duper-s3cret"
RESEND_API_KEY="re_blAh_blaHBlaHblahBLAhBlAh"
SENTRY_DSN="your-dsn"

# set this to false to prevent search engines from indexing the website
# default to allow indexing for seo safety
ALLOW_INDEXING="true"

# Amazon S3 configuration. Cloudflare Workers use scoped IAM access keys stored
# as Worker secrets because they cannot assume an EC2 instance role.
AWS_ACCESS_KEY_ID="mock-access-key"
AWS_SECRET_ACCESS_KEY="mock-secret-key"
AWS_REGION="eu-central-1"
AWS_S3_BUCKET="mock-bucket"
